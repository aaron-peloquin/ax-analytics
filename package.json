{
  "name": "ax-analytics-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nx run-many -t dev --parallel",
    "build": "nx run-many -t build",
    "test": "nx run-many -t test",
    "lint": "nx run-many -t lint",
    "resetdb": "node scripts/reset-db.js"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "nx": "^23.1.1",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
